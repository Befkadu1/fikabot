export interface Benefit {
  stat: string;
  title: string;
  text: string;
}

export interface LocationCard {
  icon: string;
  title: string;
  text: string;
}

export interface Sector {
  emoji: string;
  title: string;
  challenge: string;
  solution: string;
  benefit: string;
}

export interface Faq {
  q: string;
  a: string;
}
